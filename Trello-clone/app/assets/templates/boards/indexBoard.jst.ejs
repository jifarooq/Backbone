<h1>Your Boards</h1>

<ul class="board-gutter">
	<% boards.each(function(board) { %>
		<li id="<%= board.id %>">
			<% var showLink = "#/boards/" + board.id %>
			<h4>
				<a href="<%= showLink %>"><%= board.escape('title') %></a>
			</h4>
			<button id="delete" data-id="<%= board.id %>">
				Delete
			</button>
		</li>
	<% }); %>

	<li><a href="" id="new-board">Create new board...</a></li>
</ul>