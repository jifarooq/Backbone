TrelloClone.Models.List = Backbone.Model.extend({
	// urlRoot: "api/lists",

	// cards: function() {
	// 	this._cards == this._cards ||
	// 		new TrelloClone.Collections.

	// 	return this._cards;
	// },
});

// want to use boards route, since lists are nested under boards