TrelloClone.Models.List = Backbone.Model.extend({
	urlRoot: "api/cards",

	// cards: function() {
	// 	this._cards == this._cards ||
	// 		new TrelloClone.Collections.

	// 	return this._cards;
	// },
});
